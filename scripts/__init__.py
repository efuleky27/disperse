# Makes scripts/ a Python package so sibling scripts can import shared utilities
# (e.g. `from scripts.utils import unit_scale` when running from the project root,
# or `from utils import unit_scale` when running a script directly via python scripts/foo.py).
